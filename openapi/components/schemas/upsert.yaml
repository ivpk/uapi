type: object
properties:
  _op:
    type: string
    enum:
      - upsert
  _where:
    type: string
    pattern: ^[a-z]*$='[a-zA-Z]*$'
    examples:
      - name='Europe'
  _type:
    type: string
    examples:
      - Continet
  _value:
    type: string
    examples:
      - Europe
additionalProperties: false

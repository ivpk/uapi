description: Reference to an entry in another table
oneOf:
  - type: object
    description: maturity level > 3
    properties:
      _type:
        type: string
      _id:
        type: string
        format: uuidv4
      object:
        $ref: ./object.yaml
  - type: object
    description: maturity level >= 3
    properties:
      _type:
        type: string
      object:
        $ref: ./object.yaml

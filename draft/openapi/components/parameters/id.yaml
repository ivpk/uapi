name: id
in: path
required: true
schema:
  $ref: ../schemas/id.yaml
description: |
  Public global object identifier,  see [_id URI representation](#section/Concepts/Identifier/_id-URI-representation).

  Once object is assigned a global identifier, it should never change.
  Internally local identifiers, should be associated with public
  identifiers.

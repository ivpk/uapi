name: form
in: path
required: true
schema:
  $ref: ../schemas/ns.yaml
  enum:
    - gov
    - com
description: |
  Organization, type, can be:

  - `gov` - government organization,
  - `com` - private organization.

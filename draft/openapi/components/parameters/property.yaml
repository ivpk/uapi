name: property
in: path
required: true
schema:
  $ref: ../schemas/property.yaml
description: |
  Subresource of an object.

  All lower case, words separated with `_` symbol. Reserved properties can start with `_` symbol.
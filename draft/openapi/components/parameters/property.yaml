name: property
in: path
required: true
schema:
  type: string
  pattern: ^[a-z]([a-z]?[a-z0-9_]+)*$
  examples:
    - "cities"
description: |
  Subresource of an object.

  All lower case, words separated with `_` symbol.
name: publisher
in: path
required: true
schema:
  $ref: ../schemas/ns.yaml
  examples:
    - rc
description: |
  Abbreviation or a short code name of a publisher organization.

  All lower case, words separated with `_` symbol.

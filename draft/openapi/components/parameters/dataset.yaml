name: dataset
in: path
required: true
schema:
  $ref: ../schemas/ns.yaml
  examples:
    - dataset
    - data_set
description: |
  A dataset or a standard abbreviation or short code name.

  All lower case, words separated with `_` symbol.

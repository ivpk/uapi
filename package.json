{
  "name": "uapi",
  "version": "0.1.0",
  "dependencies": {
    "@redocly/cli": "^1.5.0"
  },
  "scripts": {
    "start": "redocly preview-docs",
    "split": "redocly split dist/bundle.yaml --outDir openapi",
    "build": "redocly bundle -o dist/bundle.yaml",
    "test": "redocly lint"
  }
}
